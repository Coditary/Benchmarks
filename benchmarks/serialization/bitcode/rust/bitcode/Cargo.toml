[package]
name = "bitcode-serialize"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
bench-support = { path = "../../../rust/bench-support", features = ["bitcode"] }
bitcode = "0.6"

[profile.release]
lto = true
codegen-units = 1
