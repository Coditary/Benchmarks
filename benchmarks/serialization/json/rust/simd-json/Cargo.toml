[package]
name = "json-serialize-simd-json"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
bench-support = { path = "../../../rust/bench-support", features = ["simd-json"] }

[profile.release]
lto = true
codegen-units = 1
