[package]
name = "json-serialize-serde-json"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
bench-support = { path = "../../../rust/bench-support" }
serde_json = "1"

[profile.release]
lto = true
codegen-units = 1
