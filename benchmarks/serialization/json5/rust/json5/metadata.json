{
  "language": "Rust",
  "implementation": "json5",
  "timing": "internal",
  "run_cmd": "./bench {size}",
  "tags": [
    "rust",
    "serialize",
    "json5",
    "text",
    "config"
  ],
  "hooks": {
    "install": "./install.sh",
    "build": "cargo build --release && cp target/release/json5-serialize ./bench",
    "clean": "cargo clean && rm -f bench"
  },
  "source_files": [
    "src/main.rs",
    "Cargo.toml"
  ],
  "artifact_path": "bench",
  "dataset": {
    "root": "datasets/shared",
    "parameter": "{domain}/{tier}",
    "input": "canonical.json"
  },
  "notes": "JSON5 encode from canonical dataset (untimed load, timed serialize)."
}
