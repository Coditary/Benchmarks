{
  "language": "Rust",
  "implementation": "bson",
  "timing": "internal",
  "run_cmd": "./bench {size}",
  "tags": [
    "rust",
    "serialize",
    "bson",
    "binary",
    "document"
  ],
  "hooks": {
    "install": "./install.sh",
    "build": "cargo build --release && cp target/release/bson-serialize ./bench",
    "clean": "cargo clean && rm -f bench"
  },
  "source_files": [
    "src/main.rs",
    "Cargo.toml"
  ],
  "artifact_path": "bench",
  "dataset": {
    "root": "datasets/shared",
    "parameter": "{domain}/{tier}",
    "input": "canonical.json"
  },
  "notes": "BSON encode from canonical dataset (untimed load, timed serialize)."
}
