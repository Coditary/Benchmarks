[package]
name = "xml-serialize-quick-xml"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
bench-support = { path = "../../../rust/bench-support", features = ["xml"] }

[profile.release]
lto = true
codegen-units = 1
