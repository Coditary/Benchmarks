[package]
name = "messagepack-serialize-rmp-serde"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
bench-support = { path = "../../../rust/bench-support", features = ["rmp-serde"] }

[profile.release]
lto = true
codegen-units = 1
