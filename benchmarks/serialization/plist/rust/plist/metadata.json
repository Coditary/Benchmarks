{
  "language": "Rust",
  "implementation": "plist",
  "timing": "internal",
  "run_cmd": "./bench {size}",
  "tags": [
    "rust",
    "serialize",
    "plist",
    "text",
    "apple"
  ],
  "hooks": {
    "install": "./install.sh",
    "build": "cargo build --release && cp target/release/plist-serialize ./bench",
    "clean": "cargo clean && rm -f bench"
  },
  "source_files": [
    "src/main.rs",
    "Cargo.toml"
  ],
  "artifact_path": "bench",
  "dataset": {
    "root": "datasets/shared",
    "parameter": "{domain}/{tier}",
    "input": "canonical.json"
  },
  "notes": "PLIST encode from canonical dataset (untimed load, timed serialize)."
}
