[package]
name = "flatbuffers-serialize"
version = "0.1.0"
edition = "2021"
publish = false
build = "build.rs"

[dependencies]
bench-support = { path = "../../../rust/bench-support" }
flatbuffers = "25"

[build-dependencies]

[profile.release]
lto = true
codegen-units = 1
