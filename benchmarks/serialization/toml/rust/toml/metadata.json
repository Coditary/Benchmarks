{
  "language": "Rust",
  "implementation": "toml",
  "timing": "internal",
  "run_cmd": "./bench {size}",
  "tags": [
    "rust",
    "serialize",
    "toml",
    "text",
    "config"
  ],
  "hooks": {
    "install": "./install.sh",
    "build": "cargo build --release && cp target/release/toml-serialize ./bench",
    "clean": "cargo clean && rm -f bench"
  },
  "source_files": [
    "src/main.rs",
    "Cargo.toml"
  ],
  "artifact_path": "bench",
  "dataset": {
    "root": "datasets/shared",
    "parameter": "{domain}/{tier}",
    "input": "canonical.json"
  },
  "notes": "TOML encode from canonical dataset (untimed load, timed serialize)."
}
