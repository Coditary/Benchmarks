{
  "language": "Rust",
  "implementation": "flexbuffers",
  "timing": "internal",
  "run_cmd": "./bench {size}",
  "tags": ["rust", "flexbuffers", "binary"],
  "hooks": {
    "install": "./install.sh",
    "build": "cargo build --release && cp target/release/flexbuffers-serialize ./bench",
    "clean": "cargo clean && rm -f bench"
  },
  "source_files": ["src/main.rs", "Cargo.toml"],
  "artifact_path": "bench",
  "notes": "flexbuffers::to_vec on shared log dataset types."
}
