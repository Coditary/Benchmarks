[package]
name = "xml-deserialize-quick-xml"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
bench-support = { path = "../../../../serialization/rust/bench-support", features = ["xml"] }

[profile.release]
lto = true
codegen-units = 1
