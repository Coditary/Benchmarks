{
  "language": "Rust",
  "implementation": "json5",
  "timing": "internal",
  "run_cmd": "./bench {size}",
  "tags": [
    "rust",
    "deserialize",
    "json5",
    "text",
    "config"
  ],
  "hooks": {
    "install": "./install.sh",
    "build": "cargo build --release && cp target/release/json5-deserialize ./bench",
    "clean": "cargo clean && rm -f bench"
  },
  "source_files": [
    "src/main.rs",
    "Cargo.toml"
  ],
  "artifact_path": "bench",
  "dataset": {
    "root": "datasets/fixtures",
    "parameter": "{domain}/{tier}",
    "input": "fixture.bin"
  },
  "notes": "Loads pre-generated wire fixture once (untimed), then measures parse + materialization."
}
