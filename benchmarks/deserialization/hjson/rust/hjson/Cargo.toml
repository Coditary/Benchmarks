[package]
name = "hjson-deserialize"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
bench-support = { path = "../../../../serialization/rust/bench-support", features = ["hjson"] }

[profile.release]
lto = true
codegen-units = 1
